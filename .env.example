# 运行模式: demo | standalone | integrated
VITE_APP_MODE=demo
# standalone/integrated 模式下后端地址
# VITE_API_BASE_URL=http://localhost:8787/api
# EDA Asset Builder Gateway；默认复用 /api，路径为 /v1/eda-builder
# VITE_EDA_BUILDER_API_BASE_URL=http://localhost:8787/api
# standalone/Vercel proxy 转发到 FastAPI Builder 服务
# EDA_BUILDER_URL=http://localhost:8000
